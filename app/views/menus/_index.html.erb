<!--div class="page-header">
  <%#= link_to new_canteen_menu_path(@canteen), :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    New Menu
  <%# end %>
  <h1>Listing menus</h1>
</div>-->

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
            <th>Date</th>
            <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <%= content_tag_for(:tr, menus) do |menu| %>
            <td><%= menu.date %></td>
            <td><%= link_to 'Show', canteen_menu_path(@canteen,menu) %></td>
        <td><%= link_to 'Edit', edit_canteen_menu_path(@canteen,menu) %></td>
        <td><%= link_to 'Destroy', canteen_menu_path(@canteen,menu), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      <% end %>
    </tbody>
  </table>
</div>
